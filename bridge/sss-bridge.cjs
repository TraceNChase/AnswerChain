#!/usr/bin/env node
// sss-bridge.cjs — Simple Shamir Secret Sharing Bridge

const fs = require("fs");
const crypto = require("crypto");

// Simple Shamir Secret Sharing implementation using GF(256)
class ShamirSecretSharing {
  constructor() {
    // Pre-computed log and exp tables for GF(256) arithmetic
    this.logTable = new Uint8Array([
      ,
    ]);

    this.expTable = new Uint8Array([
      ,
    ]);
  }

  // GF(256) arithmetic
  gfMul(a, b) {
    if (a === 0 || b === 0) return 0;
    return this.expTable[(this.logTable[a] + this.logTable[b]) % 255];
  }

  gfDiv(a, b) {
    if (a === 0) return 0;
    if (b === 0) throw new Error("Division by zero");
    return this.expTable[(this.logTable[a] - this.logTable[b] + 255) % 255];
  }

  // Evaluate polynomial at point x
  evalPoly(coeffs, x) {
    let result = 0;
    let xPow = 1;
    for (let coeff of coeffs) {
      result ^= this.gfMul(coeff, xPow);
      xPow = this.gfMul(xPow, x);
    }
    return result;
  }

  // Lagrange interpolation at x=0
  lagrange(xCoords, yCoords) {
    let result = 0;
    for (let i = 0; i < xCoords.length; i++) {
      let num = 1, den = 1;
      for (let j = 0; j < xCoords.length; j++) {
        if (i !== j) {
          num = this.gfMul(num, xCoords[j]);
          den = this.gfMul(den, xCoords[i] ^ xCoords[j]);
        }
      }
      const lagCoeff = this.gfDiv(num, den);
      result ^= this.gfMul(yCoords[i], lagCoeff);
    }
    return result;
  }

  // Split secret into shares
  split(secret, numShares, threshold) {
    if (threshold > numShares || threshold < 1) {
      throw new Error("Invalid threshold");
    }
    if (numShares > 255) {
      throw new Error("Too many shares");
    }

    const shares = [];
    for (let shareIdx = 1; shareIdx <= numShares; shareIdx++) {
      const share = new Uint8Array(secret.length + 1);
      
      for (let byteIdx = 0; byteIdx < secret.length; byteIdx++) {
        const coeffs = new Uint8Array(threshold);
        coeffs[0] = secret[byteIdx];
        for (let i = 1; i < threshold; i++) {
          coeffs[i] = crypto.randomBytes(1)[0];
        }
        share[byteIdx] = this.evalPoly(coeffs, shareIdx);
      }
      
      share[secret.length] = shareIdx; // x-coordinate
      shares.push(share);
    }
    
    return shares;
  }

  // Combine shares to reconstruct secret
  combine(shares) {
    if (shares.length === 0) throw new Error("No shares");
    
    const shareLen = shares[0].length;
    const secretLen = shareLen - 1;
    const xCoords = shares.map(s => s[secretLen]);
    
    // Check uniqueness
    if (new Set(xCoords).size !== xCoords.length) {
      throw new Error("Duplicate x-coordinates");
    }
    
    const secret = new Uint8Array(secretLen);
    for (let byteIdx = 0; byteIdx < secretLen; byteIdx++) {
      const yCoords = shares.map(s => s[byteIdx]);
      secret[byteIdx] = this.lagrange(xCoords, yCoords);
    }
    
    return secret;
  }
}

// Main execution
const main = () => {
  try {
    // Read input synchronously
    const input = fs.readFileSync(process.stdin.fd, 'utf8');
    
    if (!input.trim()) {
      console.log(JSON.stringify({ ok: false, error: "No input provided" }));
      return;
    }

    const req = JSON.parse(input);
    const sss = new ShamirSecretSharing();

    if (req.cmd === "split") {
      if (typeof req.secret_b64 !== "string") {
        throw new Error("secret_b64 missing");
      }
      if (typeof req.shares !== "number" || typeof req.threshold !== "number") {
        throw new Error("shares/threshold missing");
      }
      
      const secret = new Uint8Array(Buffer.from(req.secret_b64, "base64"));
      const shares = sss.split(secret, req.shares, req.threshold);
      const shares_b64 = shares.map(share => Buffer.from(share).toString("base64"));
      console.log(JSON.stringify({ ok: true, shares_b64 }));
      return;
    }

    if (req.cmd === "combine") {
      if (!Array.isArray(req.shares_b64)) {
        throw new Error("shares_b64 missing");
      }
      
      const shares = req.shares_b64.map(b64 => new Uint8Array(Buffer.from(b64, "base64")));
      const secret = sss.combine(shares);
      console.log(JSON.stringify({ ok: true, secret_b64: Buffer.from(secret).toString("base64") }));
      return;
    }

    throw new Error("unknown cmd");
  } catch (e) {
    console.log(JSON.stringify({ 
      ok: false, 
      error: String(e && e.message ? e.message : e) 
    }));
  }
};

// Execute main function
main();
